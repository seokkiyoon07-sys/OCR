/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // Vercel에서는 제거
  // trailingSlash: true,
  // images: {
  //   unoptimized: true
  // }
}

module.exports = nextConfig
